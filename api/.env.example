# Copie este arquivo para ".env" e preencha com a senha que você criou
# no instalador do PostgreSQL. NUNCA suba o arquivo ".env" de verdade pro
# Git (ele já está no .gitignore) — só este ".env.example" fica público.

DATABASE_URL=postgresql://postgres:SUA_SENHA_AQUI@localhost:5432/estufa_db